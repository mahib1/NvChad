local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "python" , "svelte" , "c++" , "c" , "javascript" , "typescript" , "rust" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
